<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WinWeelay.CustomControls">
    <Style TargetType="{x:Type local:NumericUpDown}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="13" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="11" />
                            <RowDefinition Height="11" />
                        </Grid.RowDefinitions>
                        <RepeatButton Grid.Column="1" Grid.Row="0" Name="PART_UpButton" FontSize="3" VerticalContentAlignment="Top">▲</RepeatButton>
                        <ContentPresenter Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"></ContentPresenter>
                        <RepeatButton Grid.Column="1" Grid.Row="1" Name="PART_DownButton" FontSize="3" VerticalContentAlignment="Bottom">▼</RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
